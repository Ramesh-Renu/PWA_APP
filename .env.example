# Production: use the API origin (do not append /api; endpoint paths already include it)
VITE_API_BASE_URL=https://your-api.example.com

# Development: keep the browser on the Vite origin and proxy these paths.
# VITE_API_BASE_URL=/
# VITE_API_PROXY_TARGET=http://localhost:5000
